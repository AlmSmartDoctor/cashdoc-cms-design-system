# pnpm v10 configuration
# Only allow build scripts for explicitly listed dependencies
# This is safer than dangerouslyAllowAllBuilds
only-built-dependencies[]=esbuild

# Ensure strict peer dependency resolution
strict-peer-dependencies=false

# Auto-install peers (recommended for library development)
auto-install-peers=true
